// > '8' == 8; // -> == is a loose comparison it will convert different types into same so the string '8' will become number 8 and then the check will be performed 8==8 true
true
// > '8' === 8; // -> this is a strict comparis and gives false because the types do not match, so the output is false
false