/**
 * the Array.prototype.join() method expects one argument that specifies the separator string.
 * In case if no argument is given, the method will default to ',' as separator.
 * In case if two or more arguments are provided, only the first one will be considered and the rest will be ignored.
 */