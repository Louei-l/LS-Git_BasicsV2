/*

let ocean = {}; // we create an empty object called ocean
let prefix = 'Indian'; // we create a varibale called prefix that hold the value of 'Indian'

ocean.prefix = 'Pacific'; // inside ocean object we create a new property called prefix and assign it value of 'Pacific'

console.log(ocean); // prefix: 'Pacific'



let ocean = {}; // we create an empty object called ocean
let prefix = 'Indian'; // we create a varibale called prefix that hold the value of 'Indian'

ocean[prefix] = 'Pacific'; // inside ocean object we create a new property called 'Indian' since we refere to it using the bracket notation and then we assign it value of 'Pacific'

console.log(ocean); // 'Indian': 'Pacific'

*/
