if (true) {
  let myValue = 20;
}

console.log(myValue); // => error.

// myValue variable is declared inside the block.
// It is limited to the scope of the block and cannot be accessed from outer scope. 
