/* What does this code output to the console?

  function isArrayEmpty(arr) {
    if(arr){
      console.log('Not Empty');
    }
  } else {
      console.log('Empty');
    }

  isArrayEmpty([]); // => Not Empty
  
  Ans: if statement evaluates only the truthiness state. 
  Only 0, NaN, undefined, null, -0, false values are falsy anythins else is true
  even an [] empty array.
  
*/
