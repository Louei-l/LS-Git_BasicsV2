// attempt without hints

function evenOrOdd (number) {
  if (number % 2 === 0) {
    console.log(`${number} is even`)
  } else {
    console.log(`${number} is odd`)
  }
}

evenOrOdd(6);

// happy it is exactly as soluction, without hints !